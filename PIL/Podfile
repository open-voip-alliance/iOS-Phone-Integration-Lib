platform :ios, '11.3'

target 'PIL' do

  source 'https://gitlab.linphone.org/BC/public/podspec.git'
  source 'https://github.com/CocoaPods/Specs.git'
  
  #//wip pod 'PhoneLib' ,:git => 'https://github.com/open-voip-alliance/iOSPhoneLib.git', 'branch' => 'main'
  #//wip pod 'PhoneLib', :path => '/Users/chris/projects/Vialer/iOS/ios-phone-lib'
  pod 'PhoneLib' ,:git => 'https://gitlab.wearespindle.com/vialer/mobile/voip/ios-phone-lib.git', 'branch' => 'main'

  target 'PILTests' do
    # Pods for testing
  end
  
  target 'PILExampleApp' do
    pod 'PIL', :path => '../PIL'
    pod 'AFNetworkActivityLogger'
    pod 'AFNetworking'
    pod 'SAMKeychain'
  end

end
